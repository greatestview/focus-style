{
  "name": "focus-style",
  "version": "2.0.0",
  "description": "Focus Style is a refined CSS base set, a reference of latest best practices and should be used as a boilerplate for building new frontends.",
  "scripts": {
    "watch": "postcss src/style.css -o dist/style.css --watch",
    "build": "postcss src/style.css -o dist/style.css"
  },
  "author": "Kim Meyer <info@meyer.kim>",
  "license": "MIT",
  "browserslist": [
    "last 1 version",
    "> 1%",
    "not dead"
  ],
  "devDependencies": {
    "postcss-cli": "^10.0.0",
    "postcss-custom-media": "^9.0.1",
    "postcss-easy-import": "^4.0.0",
    "postcss-nested": "^6.0.0"
  }
}
